# Security

Do not commit API keys, `.env` files, tokens, personal paths, user data,
production logs, or downloaded media. Report suspected vulnerabilities through
the repository's private security channel before public disclosure.

